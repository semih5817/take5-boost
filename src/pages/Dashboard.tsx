const Dashboard = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      <p className="text-foreground text-lg">Dashboard commerçant — à venir</p>
    </div>
  );
};

export default Dashboard;
